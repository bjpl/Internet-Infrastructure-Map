# API Configuration
# Copy this file to .env and fill in your API keys

# =============================================================================
# PeeringDB API Key (Optional - Public API available without key)
# =============================================================================
# Get your API key from: https://www.peeringdb.com/account/api_keys
# Public API works without key but has lower rate limits (100 requests/hour)
# With API key: 1000 requests/hour
VITE_PEERINGDB_API_KEY=

# =============================================================================
# Cloudflare Radar API Token (Required for attack/traffic data)
# =============================================================================
# Get your API token from: https://developers.cloudflare.com/radar/get-started/
# 1. Sign up at https://dash.cloudflare.com/sign-up
# 2. Go to https://dash.cloudflare.com/profile/api-tokens
# 3. Create token with "Cloudflare Radar Read" permissions
VITE_CLOUDFLARE_RADAR_TOKEN=

# =============================================================================
# TeleGeography Submarine Cable Map (No API key needed)
# =============================================================================
# Uses public GitHub data repository
# No authentication required

# =============================================================================
# CORS Proxy Configuration (Optional)
# =============================================================================
# If you encounter CORS issues, you can use a proxy service
# Options:
# - Run your own: https://github.com/Rob--W/cors-anywhere
# - Use public (rate limited): https://corsproxy.io/?
# - Serverless function (recommended for production)
VITE_CORS_PROXY=

# =============================================================================
# Data Refresh Configuration
# =============================================================================
# How often to refresh data (in milliseconds)
# Default: 300000 (5 minutes)
VITE_REFRESH_INTERVAL=300000

# Enable/disable auto-refresh
VITE_AUTO_REFRESH=true

# Enable/disable data caching
VITE_ENABLE_CACHE=true

# =============================================================================
# Rate Limiting Configuration
# =============================================================================
# Maximum requests per minute (to respect API limits)
# Future feature - not yet implemented
VITE_MAX_REQUESTS_PER_MINUTE=60

# =============================================================================
# Development/Production Settings
# =============================================================================
# Set to 'development' or 'production'
# Future feature - not yet implemented
VITE_ENV=development

# Enable detailed logging
# Future feature - not yet implemented
VITE_DEBUG=false

# =============================================================================
# Security Configuration
# =============================================================================

# Content Security Policy (CSP) - Server-side only
# For advanced CSP configurations, modify config/security-headers.config.js
VITE_CSP_NONCE=

# Enable/disable security headers in production
VITE_SECURITY_HEADERS_ENABLED=true

# Allowed origins for CORS (comma-separated, production only)
# Example: VITE_ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
VITE_ALLOWED_ORIGINS=

# Force HTTPS in production
VITE_FORCE_HTTPS=true

# Enable Subresource Integrity (SRI) checks for CDN resources
VITE_ENABLE_SRI=true

# Session timeout (milliseconds) - Default: 30 minutes
VITE_SESSION_TIMEOUT=1800000

# Enable production logging (disable for security)
VITE_ENABLE_PRODUCTION_LOGGING=false

# Sentry DSN for error tracking (optional)
# Get from: https://sentry.io/
VITE_SENTRY_DSN=

# =============================================================================
# WARNING: Security Best Practices
# =============================================================================
# 1. NEVER commit actual API keys or secrets to version control
# 2. Always use .env.local for local development with real values
# 3. Use environment-specific .env files (.env.production, .env.staging)
# 4. Rotate API keys regularly (every 90 days recommended)
# 5. Use least-privilege principle for all API keys
# 6. Monitor API usage for unusual patterns
# 7. Enable rate limiting on all public endpoints
# 8. Keep dependencies updated: npm audit && npm audit fix
